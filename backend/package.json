{
  "name": "reelhouse-backend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "uv run python -m uvicorn app.main:app --reload --port 8003",
    "dev:noreload": "uv run python -m uvicorn app.main:app --port 8003",
    "start": "uv run python -m uvicorn app.main:app --host 0.0.0.0 --port 8003",
    "setup": "uv sync"
  }
}

[build]
  base = "frontend"
  command = "npm install --ignore-scripts && npm run build"
  publish = ".output/public"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200